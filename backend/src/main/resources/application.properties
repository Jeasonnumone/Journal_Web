# 服务器配置
server.port=8081

# 数据库配置
spring.datasource.url=jdbc:mysql://localhost:3306/journal_db?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=123456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# MyBatis-Plus配置
mybatis-plus.mapper-locations=classpath:mapper/*.xml
mybatis-plus.type-aliases-package=cn.deru.backend.model
mybatis-plus.configuration.map-underscore-to-camel-case=true

# JWT配置
jwt.secret=my-super-secret-key-for-jwt-which-is-very-secure-123456asidjaoidoiasjd
jwt.expiration=86400  
# 24小时

# Swagger配置
spring.mvc.pathmatch.matching-strategy=ANT_PATH_MATCHER

# 日志配置
logging.level.cn.deru.backend=info
